'use strict';

(function (window, document, undefined) {

    var ImgsModal = new ImgsModal();

    document.addEventListener('DOMContentLoaded', () => {
        ImgsModal.init();
    });

})(window, document);